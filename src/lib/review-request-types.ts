export interface ReviewBlobReference {
  blobReference: string;
  fileName: string;
  fileSize: number;
}

export interface UploadPdfResponse {
  blobReference: string;
  fileName: string;
  fileSize: number;
}
